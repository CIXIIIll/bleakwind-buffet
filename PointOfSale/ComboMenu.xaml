<UserControl x:Class="PointOfSale.ComboMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:e="clr-namespace:BleakwindBuffet.Data.Enums;assembly=Data"
             xmlns:local="clr-namespace:PointOfSale"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="240">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Combo" TextAlignment="Center" FontWeight="Bold" FontSize="17"></TextBlock>
        <TextBlock  Text="{Binding Path = entreename}" Grid.Row="1"/>
        <TextBlock  Text="{Binding Path = sidename}" Grid.Row="2" />
        <TextBlock  Text="{Binding Path = drinkname}"  Grid.Row="3" />
    </Grid>
</UserControl>
